describe('collisionLogic', function() {
  var player, collide_object, message_box

  beforeEach(function() {
    player = new Player();
    collide_object = {id: 'npc'};
    collision_return = {'collide': true, 'object': collide_object}
  })

  it('should cause player to interact with object on collision', function() {
    // Arrange
    
    // Action

    // Assert

  }
